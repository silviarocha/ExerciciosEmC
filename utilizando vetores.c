/* exemplo de vetor */
#include <stdio.h>
#include <stdlib.h>

int main() {
    int numero[1000];
    int contador;
    int total;
    
    contador = 0;
    
    printf ("entre com um numero (escreva -1 para interromper): \n");
    scanf ("%i", &numero[contador]);
    
    while (numero[contador-1] != -1){
        total = contador
    }
}
