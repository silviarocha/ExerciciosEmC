/* exemplos de arrei: escreva um programa em c que conte os numeros e contabilize
a quantidade deles */
#include <stdio.h>

int main()
{
    int numeros[100];
    int x=0;
    int total;
    
    for(x=0; x < 100; x++)
    printf("a quantidade eh de ", x);

    return 0;
}